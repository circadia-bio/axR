% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{axivity_copy_data}
\alias{axivity_copy_data}
\title{Copy recorded data directly off a mounted Axivity volume}
\usage{
axivity_copy_data(
  device_path,
  dest_dir,
  pattern = "\\\\.cwa$",
  overwrite = FALSE
)
}
\arguments{
\item{device_path}{Character. Path to the mounted device volume, e.g.
\code{"/Volumes/CWA17_46171"}. Find this in Finder, or with
\code{list.files("/Volumes")}.}

\item{dest_dir}{Character. Destination directory for downloaded
files. Created (recursively) if it doesn't already exist.}

\item{pattern}{Character. Regex to filter which files are copied,
matched case-insensitively. Default \code{"\\\\.cwa$"}.}

\item{overwrite}{Logical. Overwrite existing files at the
destination. Default \code{FALSE}.}
}
\value{
Character vector of destination file paths that were
successfully copied.
}
\description{
A fallback for when \code{\link[=axivity_discover]{axivity_discover()}}/OMAPI device access isn't
working, but the device's USB mass-storage volume mounts and is
visible in Finder/\code{diskutil} regardless -- a common split, since the
storage side and OMAPI's own IOKit-level device discovery are
independent paths (see \code{NEWS.md} for the discovery issues hit so
far). This bypasses OMAPI and \code{device_id} entirely: it's a plain file
copy, nothing more. \code{axR} doesn't parse \code{.cwa} contents -- see
\code{mrpheus} or \code{zeitR} for that.
}
