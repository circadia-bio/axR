% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings.R
\name{axivity_get_delays}
\alias{axivity_get_delays}
\alias{axivity_set_delays}
\title{Get or set an Axivity device's delayed activation window}
\usage{
axivity_get_delays(device_id)

axivity_set_delays(device_id, start, stop)
}
\arguments{
\item{device_id}{Integer device ID, as returned by \code{\link[=axivity_discover]{axivity_discover()}}.}

\item{start, stop}{Each either a \code{POSIXct} (or coercible), \code{-Inf} (always
record from now / OMAPI's zero sentinel), or \code{Inf} (never record /
OMAPI's infinite sentinel).}
}
\value{
\code{axivity_get_delays()} returns a list with \code{start} and \code{stop},
each either a \code{POSIXct}, \code{-Inf} (OMAPI's "always"/zero sentinel), or
\code{Inf} (OMAPI's "never"/infinite sentinel).
}
\description{
Get or set an Axivity device's delayed activation window
}
