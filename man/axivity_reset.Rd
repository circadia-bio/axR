% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings.R
\name{axivity_reset}
\alias{axivity_reset}
\title{Erase an Axivity device's data storage and commit settings}
\usage{
axivity_reset(device_id, level = "quickformat")
}
\arguments{
\item{device_id}{Integer device ID, as returned by \code{\link[=axivity_discover]{axivity_discover()}}.}

\item{level}{One of \code{"none"} (commit metadata only, not recommended --
can cause a data/metadata mismatch), \code{"delete"} (remove and recreate
the data file), \code{"quickformat"} (recreate the filesystem), or
\code{"wipe"} (clear all NAND blocks, cleanest but slowest).}
}
\description{
Wraps OMAPI's \code{OmEraseDataAndCommit()}. Staged settings changes (delays,
session ID, metadata, accelerometer config) only take full effect when
this is called.
}
