% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_acttrust.R
\name{read_acttrust}
\alias{read_acttrust}
\title{Read a Condor Instruments ActTrust actigraphy file}
\usage{
read_acttrust(path, tz = "UTC", encoding = "latin1")
}
\arguments{
\item{path}{\code{character(1)} or \code{fs::path}. Path to the ActTrust \code{.txt} file.}

\item{tz}{\code{character(1)}. Time zone string passed to
\code{\link[lubridate:parse_date_time]{lubridate::parse_date_time()}}. Defaults to \code{"UTC"}. Set to the local
recording time zone for correct circadian alignment.}

\item{encoding}{\code{character(1)}. File encoding. Defaults to \code{"latin1"},
which matches Condor's default export encoding.}
}
\value{
A tibble (or plain data frame, if the \code{tibble} package isn't
installed) with one row per epoch and the following columns:
\describe{
\item{\code{datetime}}{\code{POSIXct} — epoch timestamp.}
\item{\code{activity}}{\code{double} — PIM activity count.}
\item{\code{int_temp}}{\code{double} — internal (on-body) temperature, degC.}
\item{\code{ext_temp}}{\code{double} — external (ambient) temperature, degC.
\code{NA} if unavailable.}
\item{\code{ZCMn}}{\code{double} — normalised zero-crossing mode count.
\code{NA} if unavailable.}
\item{\code{light}}{\code{double} — total light intensity (lux).
\code{NA} if unavailable.}
}
A \code{metadata} attribute is attached (a named list with \code{subject},
\code{device_id}, \code{device_model}, \code{firmware_version}, \code{interval_s},
\code{source_file}) -- the same attribute pattern used by
\code{\link[=axivity_read_cwa]{axivity_read_cwa()}}.
}
\description{
Parses a Condor ActTrust \code{.txt} export into a tidy tibble (or data frame,
if the \code{tibble} package isn't installed). The file format consists of a
variable-length key-value header block followed by semicolon-delimited
epoch rows. The header ends at the line beginning with \code{DATE/TIME}.
}
\details{
This is a device-format parser only -- it returns the file's own epoch
columns and does not add any pipeline-specific columns or classes.
Downstream packages (e.g. \code{zeitR}) are expected to wrap this in their own
function to reshape the output into their pipeline's shape, the same way
\code{zeitR::read_axivity()} currently wraps \code{\link[=axivity_read_cwa]{axivity_read_cwa()}}.
}
\examples{
\dontrun{
rec <- read_acttrust("recordings/P001.txt")
rec
attr(rec, "metadata")
}
}
\seealso{
\code{\link[=axivity_read_cwa]{axivity_read_cwa()}} for the Axivity \code{.cwa} equivalent.
}
