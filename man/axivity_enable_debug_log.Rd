% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover.R
\name{axivity_enable_debug_log}
\alias{axivity_enable_debug_log}
\title{Enable OMAPI's internal debug log}
\usage{
axivity_enable_debug_log(file = NULL)
}
\arguments{
\item{file}{Character path to a log file, or \code{NULL} (default) to log
to stderr. A file is more reliable for diagnostic purposes: OMAPI's
discovery thread logs from a background pthread, and raw stderr
writes from a non-R thread don't always reach the R console/terminal
depending on the frontend -- a file sidesteps that ambiguity.}
}
\value{
Invisibly, the OMAPI status code (negative indicates failure,
e.g. the file couldn't be opened).
}
\description{
A diagnostic escape hatch for when \code{\link[=axivity_discover]{axivity_discover()}} isn't finding a
device you expect it to. OMAPI logs internally via its own \code{OmLog()},
but axR's default log target is \code{NULL} (so compiled code doesn't write
to stderr unprompted -- see \code{NEWS.md}). This re-enables it.
}
\details{
\strong{Important:} this only controls where log lines go. Whether
anything is logged \emph{at all} is controlled by OMAPI's debug level,
which is read from the \code{OMDEBUG} environment variable once, at
\code{OmStartup()} time -- i.e. before \code{library(axR)} runs. If you're not
seeing log output after calling this, set \code{OMDEBUG} (e.g.
\code{Sys.setenv(OMDEBUG = "3")}) \emph{before} loading axR, in a fresh R
session, then call this function and retry.
}
