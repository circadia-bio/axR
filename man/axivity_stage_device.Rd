% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage.R
\name{axivity_stage_device}
\alias{axivity_stage_device}
\title{Stage an Axivity device for deployment}
\usage{
axivity_stage_device(
  device_id,
  start,
  stop = Inf,
  duration = NULL,
  session_id,
  metadata = "",
  rate = 100,
  range = 8,
  reset_level = "quickformat"
)
}
\arguments{
\item{device_id}{Character. Device identifier from \code{axivity_discover()}.}

\item{start}{POSIXct. Deployment start time. Use \code{-Inf} for "always".}

\item{stop}{POSIXct. Deployment stop time. Use \code{Inf} for "never".
Ignored if \code{duration} is supplied.}

\item{duration}{Optional. A \code{difftime} (or numeric, in seconds) giving
how long after \code{start} the deployment should run. Overrides \code{stop}.}

\item{session_id}{Integer. Session identifier for this deployment.}

\item{metadata}{Character. Free-text metadata (e.g. participant ID,
study label) written to the device.}

\item{rate}{Numeric. Accelerometer sampling rate in Hz. Default 100.}

\item{range}{Numeric. Accelerometer range in g. Default 8.}

\item{reset_level}{Character. Reset level for \code{axivity_reset()} once
settings are staged. One of \code{"delete"}, \code{"quickformat"} (default),
or \code{"wipe"}. \code{"none"} is deliberately not permitted here.}
}
\value{
Invisibly, a list of the settings that were written.
}
\description{
Configures a connected Axivity device end-to-end for a participant
deployment: accelerometer settings, deployment window (delays),
session ID, and metadata, then commits everything with a reset so
the staged settings take full effect.
}
