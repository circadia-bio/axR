% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover.R
\name{axivity_discover}
\alias{axivity_discover}
\title{Discover connected Axivity devices}
\usage{
axivity_discover()
}
\value{
A data frame with one row per connected device: \code{device_id},
\code{serial}, \code{port}, \code{path}, \code{firmware_version}, \code{hardware_version},
\code{battery_level}. Zero rows if no devices are connected.
}
\description{
Wraps OMAPI's \code{OmGetDeviceIds()} plus a handful of per-device status
calls into a single data frame. Unlike serial-port probing, this uses
OMAPI's own device discovery -- including its platform-specific finder
(IOKit/DiskArbitration on macOS, SetupAPI on Windows, udev on Linux) --
so it should behave the same way OmGui does on the same machine.
}
