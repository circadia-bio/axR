test_that("axivity_discover() is scaffolded but not yet implemented", {
  expect_error(axivity_discover())
})

test_that("axivity_open() is scaffolded but not yet implemented", {
  expect_error(axivity_open("/dev/null"))
})
